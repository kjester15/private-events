<h1>Events</h1>
<% @events.each do |event| %>
  <ul>  
    <li>
      <h3><%= event.title %></h3>
      <%= event.description %><br>
      <%= event.date %><br>
      <%= "Creator ID:" %>
      <%= event.creator.id %><br>
      <%= button_to "Attend this event", attended_event_path(event.id), method: :get %>
      <%= link_to "See Event Attendees", attended_event_path(event.id), method: :get %>
    </li>
  </ul><% end %>