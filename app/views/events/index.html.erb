<h1>Events</h1>
<h2>Upcoming Events</h2>
<% Event.upcoming.order(date: :asc).each do |event| %>
  <ul>  
    <li>
      <h3><%= event.title %></h3>
      <%= event.description %><br>
      <%= event.date %><br>
      <%= "Creator ID:" %>
      <%= event.creator.id %><br>
      <% user = user_signed_in? ? current_user.id : nil %>
      <% message = EventUser.where(attended_event_id: event.id, attendee_id: user).blank? ? "Attend this event" : "Revoke RSVP?" %>
      <%= button_to "#{message}", event_users_path, params: {id: event.id}, method: :post %>
      <%= link_to "See Event Attendees", event_path(event.id), method: :get %>
    </li>
  </ul>
<% end %>
<h2>Past Events</h2>
<% Event.past.order(date: :asc).each do |event| %>
  <ul>  
    <li>
      <h3><%= event.title %></h3>
      <%= event.description %><br>
      <%= event.date %><br>
      <%= "Creator ID:" %>
      <%= event.creator.id %><br>
      <%= link_to "See Event Attendees", event_path(id: event.id, past: "past"), method: :get %>
    </li>
  </ul>
<% end %>